# gridflow — ENTSO-E Extension Gap Closure Roadmap

Generated by `/gsd-audit-milestone` + `/gsd-plan-milestone-gaps` on 2026-04-30.
Source spec: `docs/specs/entsoe-connector-extension.md`
Audit: `docs/specs/entsoe-extension-audit.md`

---

## Completed Phases

| Phase | Name | Status | Commit |
|-------|------|--------|--------|
| Phase 1 | ENTSO-E Phase 1 silver transformers | ✅ Done | aed6cef |
| Phase 2 | ENTSO-E Phase 2 new datasets | ✅ Done | aab0a82 |
| Phase 3 | ENTSO-E Phase 3 balancing datasets | ✅ Done | 6c352c7 |

---

## Gap Closure Phases

### Phase G1 — Fix Phase 3 bronze read path + connector test
**Goal:** All 5 Phase 3 transformers must correctly locate their bronze files at runtime.
**Closes:** BLOCKER-01, MISSING-01
**Status:** ✅ Done

**Tasks:**
1. Fix `read_bronze()` path in `imbalance_prices.py`, `imbalance_volume.py`, `activated_balancing_qty.py`, `activated_balancing_prices.py`, `contracted_reserves.py` — replace doubled `entsoe/<dataset>/YYYY-MM-DD` with `year/MM/DD` relative to `self.bronze_dir`
2. Add `tests/integration/test_entsoe_connector.py` with 4 tests covering `_fetch_control_area` param correctness

---

### Phase G2 — Minor schema corrections
**Goal:** Align column names and missing fields across Phase 1/2 schemas to spec.
**Closes:** GAP-01, GAP-02, GAP-03a, GAP-05, GAP-08
**Status:** ✅ Done — 2026-04-30
**Plans:** 1 plan

Plans:
- [x] G2-01-PLAN.md — Schema contracts + transformer column alignment + test updates

**Tasks:**
1. `load_forecast` schema + transformer: add `forecast_horizon: str = "day_ahead"` literal field
2. `load_forecast_weekly` schema + transformer: add `forecast_horizon: str = "week_ahead"` literal field
3. `wind_solar_forecast` schema + transformer: rename `forecast_mw` → `generation_forecast_mw`
4. `installed_capacity` schema + transformer: rename `installed_capacity_mw` → `capacity_mw`
5. `imbalance_volume` `DOC_TYPES` entry: change `process_type="A16"` → `None`
6. Update all affected tests

**Files:**
- `src/gridflow/schemas/entsoe.py`
- `src/gridflow/silver/entsoe/load_forecast.py`
- `src/gridflow/silver/entsoe/load_forecast_weekly.py`
- `src/gridflow/silver/entsoe/wind_solar_forecast.py`
- `src/gridflow/silver/entsoe/installed_capacity.py`
- `src/gridflow/connectors/entsoe/endpoints.py`
- `tests/unit/test_entsoe.py`

---

### Phase G3 — Map balancing codes to semantic values
**Goal:** Phase 3 transformers emit human-readable direction/reserve-type strings instead of raw ENTSO-E codes; currency fields correctly named.
**Closes:** GAP-06, GAP-07
**Status:** 🔲 Planned
**Plans:** 2 plans

Plans:
- [ ] G3-01-PLAN.md — imbalance_prices + imbalance_volume schemas, transformers, tests (Wave 1)
- [ ] G3-02-PLAN.md — activated_balancing_qty + activated_balancing_prices + contracted_reserves schemas, transformers, fixtures, tests (Wave 2)

**Tasks:**
1. `imbalance_prices`: add `direction` column ("long"/"short") mapped from A19→long, A20→short; rename `price_gbp_mwh` → `price_eur_mwh`; update schema
2. `imbalance_volume`: add `direction` column mapped from flow_direction A01→long, A02→short; rename `volume_mwh` stays; update schema field `flow_direction` → `direction`
3. `activated_balancing_qty`: add `reserve_type` (A95→"fcr", A96→"afrr", A97→"mfrr") + `direction` (A01→"up", A02→"down") columns; update schema
4. `activated_balancing_prices`: same reserve_type + direction mapping; rename `price_gbp_mwh` → `price_eur_mwh`; update schema
5. `contracted_reserves`: add `reserve_type` column mapped from businessType; rename `quantity_mw` stays; update schema
6. Update all Phase 3 schemas with new typed fields
7. Update all Phase 3 transformer tests (new column assertions)
8. Update Phase 3 fixture XMLs if needed to exercise direction/reserve_type paths
9. Add `ingested_at` to all 5 Phase 3 transformers and schemas (deferred tech debt)

**Files:**
- `src/gridflow/schemas/entsoe.py`
- `src/gridflow/silver/entsoe/imbalance_prices.py`
- `src/gridflow/silver/entsoe/imbalance_volume.py`
- `src/gridflow/silver/entsoe/activated_balancing_qty.py`
- `src/gridflow/silver/entsoe/activated_balancing_prices.py`
- `src/gridflow/silver/entsoe/contracted_reserves.py`
- `tests/fixtures/entsoe/activated_balancing_qty_gb.xml`
- `tests/fixtures/entsoe/activated_balancing_prices_gb.xml`
- `tests/unit/test_entsoe.py`

---

### Phase G4 — outages_generation unit-level schema
**Goal:** Replace zone-aggregate outages silver with the unit-level schema the spec requires.
**Closes:** GAP-04
**Status:** 🔲 Planned (larger effort ~1 day)

**Tasks:**
1. Extend `parse_timeseries_xml` to extract `RegisteredResource.mRID`, `RegisteredResource.name`, `businessType` (A53=planned/A54=unplanned) from A80 `Unavailability_MarketDocument` TimeSeries elements
2. Redesign `EntsoeGenerationOutage` schema: `timestamp_utc`, `area_code`, `unit_mrid: str`, `unit_name: str = ""`, `outage_type: str # "planned"/"unplanned"`, `unavailable_mw: float`, `resolution: str = ""`
3. Rewrite `OutagesGenerationTransformer.transform()` for unit-level dedup on `(timestamp_utc, unit_mrid)`
4. Create new A80 fixture XML with `RegisteredResource` elements (businessType A53/A54)
5. Update unit tests for new schema shape

**Files:**
- `src/gridflow/connectors/entsoe/parsers.py`
- `src/gridflow/schemas/entsoe.py`
- `src/gridflow/silver/entsoe/outages_generation.py`
- `tests/fixtures/entsoe/outages_generation_gb.xml`
- `tests/unit/test_entsoe.py`

**Note:** This changes the silver schema for an existing dataset. Any downstream gold views consuming `outages_generation` silver will break and need updating.

---

## Deferred Tech Debt

| Item | Decision |
|------|----------|
| `DEFAULT_CONTROL_AREAS = ["GB"]` only | Intentional UK scope; expand when continental data is needed |
