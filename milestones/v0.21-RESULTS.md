# v0.21 results

## L — Vintage stamps (implementation stage, 2026-09-07)

Implemented system-price vendor publication stamps, the dated MID 35-minute policy,
publication-fallback accounting, the declaration-driven positive-offset transform-window
warning, scoped rebuild clocks, and the corrected independent system-price oracle.

Repository verification is green: strict lint/type checks pass, the focused L/affected suites
pass 103 tests, and the full non-live/non-slow gate passes 2,688 tests (187 skipped,
265 deselected).

No live ingest or transform was run. Production acceptance measurements and disk rebuilds are
intentionally pending the dispatcher/seat's post-merge operations. In particular, no claim is
made yet for production fallback count, yearly availability distributions, fixed-cohort
conservation, doubled-key winners, MID full-tree uniqueness/completeness, or refreshed benchmark
counts. The implementation oracle requires zero rows with `published_at < event_time`; any such
row fails with its raw body disclosed.
